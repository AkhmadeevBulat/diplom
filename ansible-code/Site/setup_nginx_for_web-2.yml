---
- name: Setup user and deploy web pages with NGINX on Ubuntu
  hosts: web-2
  become: true

  tasks:
    - name: Update apt package index
      apt:
        update_cache: yes

    - name: Install NGINX
      apt:
        name: nginx
        state: present

    - name: Start and enable NGINX service
      service:
        name: nginx
        state: started
        enabled: true

    - name: Create web page for web-2
      template:
        src: web2_template.html.j2
        dest: /var/www/html/index.html
      when: inventory_hostname == "web-2"
